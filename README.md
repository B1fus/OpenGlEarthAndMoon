# Install
1. 
```cmake
cmake .
cmake --build . --config release
```
2. Go to folder with exe file (.../Release or .../Debug if you choose "-\-config debug") and copy freeglut.dll and glew32.dll there.
3. Run Main.exe
